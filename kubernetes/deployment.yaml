apiVersion: apps/v1
kind: Deployment
metadata:
  name: chatting-backend
  labels:
    app: chatting-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chatting-backend
  template:
    metadata:
      labels:
        app: chatting-backend
    spec:
      containers:
        - name: chatting-backend
          image: 357806352922.dkr.ecr.ap-northeast-2.amazonaws.com/moine_chatting:latest
          ports:
            - containerPort: 8080
          env:
            - name: DATABASE_URL
              valueFrom:
                configMapKeyRef:
                  name: chatting-configmap
                  key: database_url
            - name: DATABASE_USERID
              valueFrom:
                configMapKeyRef:
                  name: chatting-configmap
                  key: database_userid
            - name: DATABASE_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: chatting-configmap
                  key: database_password
            - name: KAFKA_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: chatting-configmap
                  key: kafka_endpoint
            - name: SERVER_PORT
              valueFrom:
                configMapKeyRef:
                  name: chatting-configmap
                  key: server_port
            - name: PING_MSG
              valueFrom:
                configMapKeyRef:
                  name: chatting-configmap
                  key: ping_msg
#          readinessProbe:
#            httpGet:
#              path: '/actuator/health'
#              port: 8080
#            initialDelaySeconds: 10
#            timeoutSeconds: 2
#            periodSeconds: 5
#            failureThreshold: 10
#          livenessProbe:
#            httpGet:
#              path: '/actuator/health'
#              port: 8080
#            initialDelaySeconds: 120
#            timeoutSeconds: 2
#            periodSeconds: 5
#            failureThreshold: 5
